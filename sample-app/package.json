{
  "name": "sample-app",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny TypeScript app used to demo PRs that the sandboxed AI reviewer comments on.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
